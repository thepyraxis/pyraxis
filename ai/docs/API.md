# API

UNKNOWN — no backend API, route handlers, or external integrations are defined in any of the five source manuals. The manuals describe the Growth Engines (Lead, Booking, Follow-up, Retention, Review, Referral, Intelligence — see `ai/context/03-website.md`) conceptually and visually, but do not specify their actual API contracts, endpoints, or data schemas.

## When this becomes real

Document here, per endpoint: method, path, request/response schema, auth requirement, which Growth Engine it belongs to, and which section/component consumes it. Mirror the structure in `ai/knowledge/` once concrete.

## Related

`ai/context/03-website.md`, `ai/rules/security.md`
