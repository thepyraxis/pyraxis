# Index — Animations

None implemented yet. UNKNOWN.

Once GSAP timelines / Three.js animations exist, list every one here as:

```
### AnimationName
- Trigger: e.g. ScrollTrigger on #hero
- Dependencies: ...
- Performance notes: ...
```

Keep in sync with `ai/knowledge/animations.json`.

## Known transition map (spec, not yet built)

Hero → Problem → Growth System → Growth Engines → Portfolio → Process → Future-Proof Systems → CTA → Footer, each a physical particle transformation, never an opacity crossfade. Full detail: `ai/knowledge/animations.json` → `transitionMap`, source `ai/context/08-animation-system.md`.

## Related

`ai/prompts/animate.md`, `ai/rules/animation.md`
